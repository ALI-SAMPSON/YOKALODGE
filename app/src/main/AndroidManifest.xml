<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.icode.yokalodge"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon_1_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.HomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Sign_up">
        </activity>

        <activity android:name=".activities.AdminDashBoardActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.AdminDashBoard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SignUpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Sign_up"></activity>
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Sign_up"></activity>
        <activity
            android:name=".activities.SplashScreenActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".activities.AdminLoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.AddRoomsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/homeTheme">
        </activity>
        <activity android:name=".activities.DeleteRoomsActivity" />
        <activity android:name=".activities.EditCredentialsActivity" />
        <activity android:name=".activities.CheckPaymentInfoActivity" />
        <activity android:name=".activities.AboutUsUserActivity" />
        <activity android:name=".activities.AboutUsAdminActivity" />
        <activity android:name=".activities.ViewAddedRoomsActivity" />
        <activity android:name=".activities.MakePaymentActivity" />
        <activity android:name=".activities.SliderViewActivity" />
        <activity android:name=".activities.ResetPasswordActivity" />
        <activity android:name=".activities.ResetAdminPasswordActivity">

        </activity>
    </application>

</manifest>